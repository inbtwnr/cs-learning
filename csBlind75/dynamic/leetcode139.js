/**
 * @param {string} s
 * @param {string[]} wordDict
 * @return {boolean}
 */
var wordBreak = function(s, wordDict) {
    const dp = new Array(s.length + 1).map(() => false)

    
    for (let i = s.length; i > 0; i--) {
        for (let w of wordStr) {
            if ((i + w.lenght) <= s.length)
        }      
    }
};